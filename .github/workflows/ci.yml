name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends librdkafka-dev

      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Ruff (lint)
        run: ruff check .

      - name: Mypy (types)
        run: mypy backend/ || true   # not strict yet; don't fail the build

      - name: Unit tests
        run: pytest tests/unit -v

  # Integration + ML jobs land in phases 2/3.
  # integration:
  #   runs-on: ubuntu-latest
  #   needs: lint-and-test
  #   steps:
  #     - uses: actions/checkout@v4
  #     - run: docker compose up -d postgres redis kafka
  #     - run: pytest tests/integration -v
