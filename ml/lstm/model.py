"""PyTorch LSTM architecture. Phase 3.

Kept as a stub so tests under tests/ml/ have something to import once
torch is added to requirements.
"""

# TODO(phase-3): BehaviorLSTM(nn.Module) — 18 in, hidden=64, 2 layers,
# attention over timesteps, sigmoid output.
